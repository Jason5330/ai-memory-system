# 生活化測試流程（親手驗收每個機制）

> 用最白話的生活例子，從零走一遍，親眼看到每個機制都正常。約 15 分鐘。
> 每關都有「✅ 怎麼算過關」。可以邊做邊在 `☐` 打勾。
>
> 📌 前提：已跑過 `install-personal`（個人層）。本測試走**個人腦**（不在專案裡），所以記憶在
> `%USERPROFILE%\.ai-memory`。路徑裡的 `$env:USERPROFILE` 會自動變成你的家目錄，直接複製貼上即可；
> 要打開檔案就把 `%USERPROFILE%\.ai-memory\...` 貼到檔案總管網址列。

---

## 🎬 情境

把 Claude 當成你的私人小幫手。下面每一步都是日常對話，照順序做。

---

## ☐ 站 0：開個全新對話

保險起見，先**重開一次 VS Code**，然後開一個新對話再開始。

---

## ☐ 站 1：一段對話，一次儲存，驗證 3 個機制

**先自然地跟它講這三句話（一次講完）：**

```
①「記住我的習慣：回答我的時候簡短一點，先講重點就好。」
②「順便記一下：我家巷口的『阿香早餐店』星期一公休，他們的蛋餅最好吃。」
③「另外幫我寫一首給朋友的生日祝福短詩。」
```

**然後打：** `/capture`

**你會看到 + ✅ 三個過關點：**

```
✅ A 偏好有記到
   → 打開 %USERPROFILE%\.ai-memory\conversations\今天日期.md
   → 看到「簡短、先講重點」這條

✅ B「阿香早餐店」變成一張記憶卡（值得記的知識）
   → PowerShell 打：ls "$env:USERPROFILE\.ai-memory\knowledge\"
   → 看到多了一張「阿香早餐店」的卡，裡面寫著「星期一公休、蛋餅好吃」

✅ C 那首生日詩「沒有」變成記憶卡（你做出來的東西，不是知識）
   → 同一個資料夾裡，找不到那首詩的卡
   → 證明：系統會分辨「知識」和「你做出來的成品」，不亂存垃圾
```

---

## ☐ 站 2：壞掉的功能會被自動擋

> 白話：Claude 做事會用到一些「功能」。萬一某個功能在你電腦上壞了，
> 這套系統會記住它、以後**自動擋住**，不讓它再白白去踩。

**講給它聽 + 存起來：**

```
對話：「假裝有一個叫【舊地圖】的功能在我電腦上壞了，以後不要用它。」
動作：/capture
✅ 過關：打開 %USERPROFILE%\.ai-memory\blocked-actions.json
        看到清單裡多了「舊地圖」這一筆
```

**親眼證明它真的擋得住（貼這一行到 PowerShell，按 Enter）：**

```powershell
'{"tool_name":"舊地圖"}' | powershell -NoProfile -File "$env:USERPROFILE\.ai-memory\hooks\block-failed-actions.ps1" claude; "結果代碼=$LASTEXITCODE"
```

```
你會看到：一行「擋下」的訊息 + 結果代碼=2
✅ 過關：代碼是 2 = 成功擋下（0 是放行）
```

---

## ☐ 站 3：看一眼總覽

```
動作：/status
✅ 過關：看到記憶庫狀況、進度——數字跟你前面存的對得上
```

---

## ☐ 站 4：自動健康檢查（程式在檢查，不靠 Claude 判斷）

```
動作（PowerShell）：& "$env:USERPROFILE\.ai-memory\memory-lint.ps1"
✅ 過關：跑完最後一行沒有「FAIL」（有「WARN」沒關係）
```

---

## ☐ 站 5：讓它「沉澱 + 反省」，提出一條規矩 ⭐ 重點

```
動作：/dream
你會看到：它把這陣子的對話整理、反省一遍，並印出一份報告
```

**跑完去哪看證據？兩個地方：**

```
① 對話視窗本身 → /dream 印的報告（最快的第一手證據）

② 打開這個檔（站 5 過關關鍵）：
   📂 %USERPROFILE%\.ai-memory\doctrine_candidates.md
   ✅ 過關 = 最底部多出一段「## 待審核準則 今天日期」，
            裡面有一條新候選，內容大概是「回答先講結論 / 簡短講重點」

③ 順便確認反省日誌也更新：
   📂 %USERPROFILE%\.ai-memory\reflection.md
   ✅ 加分 = 多出一段「## 反思紀錄 今天日期」
```

> ⚠️ **重要**：這兩個檔本來就有舊內容（以前測試留下的，日期是更早的）。
> 看的時候**只認今天的日期**、直接拉到檔案**最底部**，最新的永遠在最下面。

> 如果底部沒長出今天的新候選 → 表示「簡短回答」這習慣只出現一次，還不夠強。
> 補做：再開一段對話，故意等它回得落落長 → 跟它說「太長了，簡短點」→ 再 `/capture` → 再 `/dream`。
> 同一習慣出現兩次，就會變成候選。

---

## ☐ 站 6：你親自批准那條規矩

```
動作：/review-doctrine → 對「先講結論」那條回「批准」
✅ 過關：打開 %USERPROFILE%\.ai-memory\doctrine.md
        看到多了一條已批准的規矩
```

---

## ☐ 站 7：驗證「這條規矩真的改變了它」← 整套的終點

```
動作：開一個全新對話，隨便問一個要解釋的問題，例如「為什麼天空是藍色的？」
✅ 過關：它「自動先講結論、再講細節」——你沒提醒，它卻照做了
        = 你站 1 的一句話，已經變成它永久的習慣 🎉
```

---

## ☐ 站 8：設定每天自動整理（可選）

```
動作：/schedule-dream → 選個時間（例如晚上 10 點）
✅ 過關：看到「已設定」的確認
```

---

## 🧹 玩完想還原（可選）

```
1. blocked-actions.json 刪掉「舊地圖」那筆
2. 不想留的：刪今天的 conversations\今天.md、knowledge 裡的「阿香早餐店」卡
（其他都可以留著，它們本來就是你的記憶）
```

---

## 一張圖看懂整條路

```
站1  跟它講 3 句話 → /capture
       ├─ 偏好記下來
       ├─ 早餐店變知識卡
       └─ 生日詩被篩掉（不亂存）
站2  講「舊地圖壞了」→ 自動擋住（代碼=2）
       ↓
站3 看總覽   站4 健康檢查
       ↓
站5 /dream 反省 → 提出規矩候選（看 doctrine_candidates.md 最底部）
       ↓
站6 你批准（doctrine.md 多一條）
       ↓
站7 新對話它自動照做 ← 記憶真的改變了未來 ✅
```
