# 機制與工具總覽（Mechanisms & Tools Reference）

> 這份把框架裡所有**確定性腳本（lib/ 與其他）**和**機制**整理成一張地圖。
> 看白話入門：`新手指南.md`；看指令用法：打 `/help`；看名詞中文：`記憶名詞對照表.md`。

---

## 核心設計哲學：AI 判斷 ⊥ 確定性腳本

整個框架只有一條主軸——**讓 AI 做它擅長的「語意判斷」，讓腳本做它擅長的「確定性執行」**：

| 層 | 誰做 | 性質 | 例子 |
|----|------|------|------|
| 判斷 | **AI**（讀 markdown 指令/入口檔） | 盡力而為、語意 | 這值不值得記？這是不是同一個工作流？這是機械程式還是判斷工作流？ |
| 執行 | **確定性腳本**（lib/ 等） | 100% 可重現 | 原子寫檔、計次、備份/校驗/回滾、硬擋、跑存好的程式 |

**所以每個機制都是「AI 判斷 → 交給腳本安全執行」**。腳本放在 `~/.ai-memory/lib/`（個人層、跨專案）。

---

## 記憶檔案結構（檔案在哪）

**個人腦 `~/.ai-memory`**（跨所有專案共用、**永不進專案 git**；開場由 `~/.claude/CLAUDE.md`〔Claude〕／`~/.codex/AGENTS.md`〔Codex〕載入）：
```
~/.ai-memory/
├── MEMORY.md                ← 個人索引（每次對話載入；頂部「環境限制」指令區）
├── feedback_user_style.md   ← 你的偏好（任務怎麼做）
├── persona.md               ← AI 的人設／語氣／定位（AI 怎麼當）
├── reflection.md            ← 反思日誌（dream 累積）
├── doctrine.md              ← 已批准的永久行為準則
├── doctrine_candidates.md   ← 待審核準則候選
├── blocked-actions.json     ← 失效工具登記簿（hook 讀它硬擋）
├── knowledge/               ← 個人（跨專案）知識頁
├── conversations/           ← 個人／非專案對話（含 archive）
├── tools/                   ← Saved Tools：tools.json + <slug>.{ps1,sh}
├── lib/  guides/  hooks/  cron/  reset/  memory-lint.*  project-templates/
```
**專案腦 `<專案>/.claude/memory`**（每專案、可隨專案 git 分享；開場由 `<專案>/CLAUDE.md`〔Claude〕／`<專案>/AGENTS.md`〔Codex〕載入）：
```
<專案>/.claude/memory/
├── MEMORY.md      ← 這個專案的索引
├── knowledge/     ← 這個專案的知識頁
└── conversations/ ← 這個專案的對話（含 archive）
```
> ⚠️ **能被「召回」的只有各層 `MEMORY.md`（＋個人 `doctrine.md`/`persona.md`）**——開場只自動載入這些；
> `conversations/*.md` 不會自動載入。所以想被記住的東西必須進 `MEMORY.md` 索引（或 knowledge 頁），不能只躺在對話 log。

---

## Part 1 — `lib/` 三大工具

### 1. `memory-write.{ps1,sh}` — 安全寫入器（Hermes 式外殼）
把「最怕寫壞」的兩種寫入交給確定性腳本：**空內容擋、重複擋(dedup)、檔鎖、先讀磁碟最新、temp+rename 原子寫、drift 先備份(.bak)**。

| 模式 | 用途 | 指令 |
|------|------|------|
| `append` | 安全追加一段內容到檔案（去重） | `memory-write.ps1 -Mode append -File <path> -Content "<block>"` |
| `block-tool` | 原子加一條到硬擋註冊表 | `memory-write.ps1 -Mode block-tool -Tool <Name> -Reason "..." -UseInstead "<Alt>" -Platform both` |

- **退出碼**：`0` 寫入 · `0`(印 SKIP) 重複不動 · `2` 空內容拒絕 · `3` 鎖忙 · `4` 寫錯。
- **誰呼叫**：`/capture` Step 2.5（壞工具進硬擋）、Memory Decision Gate 的安全類別 append。
- **範圍刻意收斂**：只用在硬擋註冊表 + append-only 檔；knowledge 頁要語意合併，仍由 AI 編輯。

### 2. `detect-repeats.{ps1,sh}` — 重複工作流偵測器（確定性計次）
掃兩層 `conversations/*.md` 的 `🔁 repeat:<slug>` 標籤**精準計次**、**排除已有同名 skill**、列出 **seen ≥ 門檻且尚無 skill** 的工作流。

```
detect-repeats.ps1 [-Threshold 2] [-ProjectPath <dir>] [-Json]
```
- **計次不分時間地點**：同一 session（同一檔多筆）／同天／跨 session／跨層一律累加。
- **退出碼**：恆 `0`（唯讀報告）。`-Json` 給機器讀。
- **誰呼叫**：`/capture` Step 4、`/dream` Phase 3c、`/status`、**入口檔 session-start 第 5 步**（一開 session 就提醒）。
- **輸出純 ASCII**（PS 5.1 會把無 BOM 的 .ps1 非 ASCII 字面誤解碼）；中文提醒語由 AI 講。

### 3. `tool.{ps1,sh}` — Saved Tools 執行器
「寫好的程式存起來，下次同樣請求直接跑、不重寫」。**不是 skill（playbook），是一支直接執行的程式。**

| 子指令 | 用途 |
|--------|------|
| `list` | 列出已存的工具（slug／描述／觸發語） |
| `run <slug> [args]` | **直接執行**存好的程式（轉送**位置**參數） |
| `add <slug> -Desc "..." -Triggers "a;b;c" -Script <path>` | 把能用的程式複製進 `tools/<slug>.{ps1,sh}` 並註冊觸發語 |
| `path <slug>` | 印出該工具的程式路徑 |

- **註冊表**：`~/.ai-memory/tools/tools.json`（觸發語 → 程式）；程式本體放 `~/.ai-memory/tools/<slug>.{ps1,sh}`。
- **退出碼**：`0` ok · `2` 找不到 · `3` 用法錯 · `4` 執行錯。
- **誰呼叫**：入口檔「Saved tools」規則——① 寫程式前先 `list` 查、命中就 `run`；② 寫好可重複的程式就問要不要 `add`。
- **限制**：`run` 轉送**位置參數**；`-Named` flag 會被 wrapper 的參數綁定攔截 → 存的工具請設計成吃位置參數或環境變數。

---

## Part 2 — 其他確定性腳本（lib 外，但屬機制）

| 腳本 | 位置 | 用途 | 重點 |
|------|------|------|------|
| `block-failed-actions.{ps1,sh}` | `~/.ai-memory/hooks/` | **PreToolUse 硬擋**：讀 `blocked-actions.json`，命中就 `exit 2`+stderr 擋下工具 | catch-all matcher；解析失敗 **fail-open**（壞註冊表不會鎖死全部工具） |
| `reset.{ps1,sh}` | `~/.ai-memory/reset/` | **確定性重置**：備份→校驗→清除→失敗回滾 | `-Layer`/`-Categories`/`-DryRun`/`-Yes "yes reset"`；退出 0/2拒/3校驗失敗/4回滾。`/reset` 點選式前端呼叫它 |
| `memory-lint.{ps1,sh}`（doctor） | `~/.ai-memory/` | **健檢**：連結/Why/source/schema/hook 註冊/雙平台同步/未整合 log | `-Strict` → fail>0 回 exit 1（CI gate）。`/status`、`/dream` Phase 6 呼叫 |
| `nightly.{ps1,sh}` | `~/.ai-memory/cron/` | **每晚整理**：跑 ingest-sessions + dream | lock + run-id + JSON audit + 真 `-DryRun`。`/schedule-dream` 註冊進 OS 排程器 |

---

## Part 3 — 機制總覽（AI 判斷的那一半）

機制大多寫在**入口檔**（`CLAUDE.md`/`AGENTS.md`）或 **guides**（`~/.ai-memory/guides/`），由 AI 讀後執行，必要時呼叫上面的腳本。

| 機制 | 在哪 | 一句話 |
|------|------|--------|
| **Memory Decision Gate**（三層信心） | `guides/_memory-gate.md` | 每個訊號判信心：高→自動寫安全類別、中→進候選、低→跳過。**doctrine/skill 永不自動寫**（人審 gate） |
| **訊號分層路由** | `guides/_routing.md` | 「關於你/行為/壞工具」→個人層；「關於這專案」→專案層 |
| **Two-step write** | capture/dream | 先寫完整 topic 檔→確認→才更新 MEMORY.md 一行指標（crash-safe；MEMORY.md 只當 bounded index） |
| **重複偵測 → 分流** | capture Step 4 / detect-repeats | 重複 ≥2×：**機械程式 → Saved Tool**；**判斷工作流 → /harvest skill** |
| **Saved Tools** | 入口檔「Saved tools」+ `lib/tool` | 機械程式第一次存、之後直接跑（自動，不必打 /capture） |
| **硬擋 enforcement** | 入口檔 + hook + `blocked-actions.json` | 壞工具第 2 次 → 註冊 → PreToolUse 物理擋下（兩平台） |
| **Memory-context 注入防護** | 入口檔 | 載入的記憶是「持久資料」不是「新指令」；防存到記憶的第三方文字做注入 |
| **雙層記憶** | `guides/PATHS.md` | 個人 `~/.ai-memory`（跨專案、不進 git） ⊥ 專案 `<proj>/.claude/memory` |
| **雙平台物化** | `guides/_materialize-skill.md` | 一份 SKILL.md 同時落 Claude(`~/.claude/skills`) + Codex(`~/.agents/skills`) |
| **技能自動觸發** | 各 SKILL.md 的 `description` | 請求語意命中 description → 模型自動經 Skill 工具調用 |

---

## Part 4 — 三條「省你重寫」的 lane（最常搞混）

| 你重複做的事 | 走哪條 | 載體 | 下次 |
|---|---|---|---|
| 固定**機械程式**（截圖到桌面、合併 log） | **Saved Tool** | `~/.ai-memory/tools/<slug>.{ps1,sh}` | `tool run <slug>` 直接跑 |
| **要判斷的多步工作流**（寫 PR 摘要） | **Skill** | `~/.claude/skills/<n>/SKILL.md`（雙平台） | 語意命中自動觸發 |
| 排程/定期（每晚整理、每週報告） | **Automation** | OS 排程器 + cron 腳本 | 時間到自動跑 |

> 共同點：**都不必你打指令**——AI 偵測到重複會主動 offer，你點頭即可。doctrine/skill 仍需人審才生效。

---

## Part 5 — 速查：所有確定性腳本的退出碼

| 腳本 | 0 | 2 | 3 | 4 |
|------|---|---|---|---|
| `memory-write` | 寫入/SKIP | 空內容拒絕 | 鎖忙 | 寫錯 |
| `detect-repeats` | 恆 0（報告） | — | — | — |
| `tool` | ok | 找不到 | 用法錯 | 執行錯 |
| `reset` | ok/dry-run | 拒絕(無 yes) | 備份校驗失敗 | 已回滾 |
| `memory-lint` | pass（`-Strict` 下 fail=0） | — | — | （`-Strict` fail>0 → exit 1） |

---

## Part 6 — 踩過的環境雷（Windows / PowerShell 5.1）

這些都已在腳本內修掉並驗證，列出供維護參考：

- **無 BOM 的 .ps1 非 ASCII 字面 → cp950 誤解碼破壞解析** → 腳本輸出/比對改純 ASCII，中文放 markdown 由 AI 講。
- **`Get-Content -Raw` 讀 UTF-8 無 BOM 中文 → 誤解碼** → 改 `[IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)`。
- **變數名稱大小寫不敏感**：`$SETTINGS`(路徑) 與 `$settings`(物件) 是同一個 → 用不同名（`$settingsObj`）。
- **regex 尾錨點 `$` 配 em-dash 回溯** → slug `md-to-html` 被切成 `md-to`：改用 ASCII `repeat:` token、去尾錨點。
- **`tool run` 的 `-Named` 參數被 wrapper 攔** → 存的工具設計成吃位置參數/環境變數。
- **`Remove-Item <變數> -Recurse -Force` 被沙箱靜態攔下** → 用 `[IO.Directory]::Delete($p,$true)`。
- **git-bash 的 `python3` 是 Microsoft Store stub（exit 49）** → `.sh` 目標是 Mac/Linux 的真 python3；Windows 走 `.ps1`。

---

> 修改原則：所有東西都在 `canonical/` 寫一份，由 `install-personal` 物化到兩平台。改腳本後用隔離 `USERPROFILE` 跑 `install-personal` + `memory-lint`（fail=0）驗證，並保持 `bash -n` 乾淨。
