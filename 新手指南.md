# 新手指南（白話版）

> 給完全沒用過的人。看完這份就會用。想看更深的：[`機制與工具總覽.md`](機制與工具總覽.md)（機制／lib 工具／CLI 速查）。

---

## 一句話：這是什麼？

平常每開一個新對話，AI 就**失憶**，你得重講一次你是誰、上次做到哪。
裝了這套之後，**Claude Code 或 Codex** 會**記住**你的偏好、決策、知識，而且**越用越懂你**。

```
沒裝：你 →「幫我做 X」→ AI（完全不認識你）
裝了：你 →「幫我做 X」→ AI 先讀記憶 → 知道你是誰、你的習慣 → 給更準的答案
```

## 記憶分兩層（重要觀念）

- **個人腦**（`~/.ai-memory`）：你的偏好、行為準則、反思——**所有專案共用、越來越懂你**，永不進專案 git。
- **專案腦**（`<專案>/.claude/memory`）：那個專案自己的知識、對話。

---

## 先拿到框架

```powershell
git clone https://github.com/Jason5330/ai-memory-system.git
```
這樣會得到資料夾 **`ai-memory-system`**（下面範例都用這個名字）。
> 若你是按 GitHub 綠色按鈕「**Download ZIP**」下載解壓，資料夾會叫 **`ai-memory-system-main`**——
> 那下面範例的 `ai-memory-system` 請自行換成 `ai-memory-system-main`。

---

## 安裝：兩個指令，別搞混

不是「裝兩次同一個東西」，是兩件不同的事：

| | 跑幾次 | 在哪跑 | 給你什麼 |
|---|---|---|---|
| `install-personal.ps1` | **每台電腦一次** | 框架資料夾裡 | 你的**個人腦**（全域共用）|
| `init-project.ps1` | **每個新專案各一次** | **新專案資料夾裡** | 那個專案自己的**專案腦** |

**順序：先 install-personal、後 init-project**（init-project 會讀 install-personal 先放好的範本）。

```powershell
# 1) 每台電腦一次：在框架資料夾裡跑
.\install-personal.ps1            # Windows
./install-personal.sh             # Mac/Linux

# 2) 每開一個新專案：進到「那個專案資料夾」再跑（不是框架資料夾！）
cd C:\Users\你的帳號\Desktop\我的新專案
C:\Users\你的帳號\Desktop\ai-memory-system\init-project.ps1
```

> ⚠️ 出現「無法執行指令碼」紅字 → 先貼這行再重試：
> ```powershell
> Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
> ```

裝完**重啟 Claude Code / Codex**。只是隨便聊、沒有專案 → 不用跑 init-project，個人腦已經能用。

---

## 實戰：框架夾放在專案裡，在 VSCode 怎麼下指令

```
我的新專案\                          ← VSCode 開在這層（要初始化的是這層）
└── ai-memory-system\     ← 框架夾放進來了
```

1. 按 **Ctrl + 反引號鍵**（鍵盤左上角、`Esc` 正下方、數字 `1` 左邊那顆）開內建終端機（或選單 Terminal → New Terminal），
   確認提示字元是 `PS C:\Users\你的帳號\Desktop\我的新專案>`。
2. 第一次先裝個人層：
   ```powershell
   powershell -ExecutionPolicy Bypass -File .\ai-memory-system\install-personal.ps1
   ```
3. 初始化這個專案：
   ```powershell
   powershell -ExecutionPolicy Bypass -File .\ai-memory-system\init-project.ps1
   ```
跑完專案根目錄會長出 `CLAUDE.md`、`AGENTS.md`、`.claude\`、`.codex\`、`.agents\`、`.gitignore` = 成功。

> 地雷：**別先 `cd` 進框架子夾再跑**（會把記憶建錯地方）；框架夾放專案裡會被 git 進去，介意就在
> `.gitignore` 加 `ai-memory-system/` 或把框架夾放專案外面。

---

## 九個指令（打在對話框，像打字一樣）

| 指令 | 白話 | 何時用 |
|------|------|--------|
| `/capture` | 把這次對話的重點存起來 | 聊到值得記的、或某工具壞了 |
| `/ingest-sessions` | 回頭把最近會話沒記到的補抓進來 | 怕漏記；或交給每晚自動 |
| `/dream` | 大整理 + 反思 + 提煉準則候選 | 累積幾天後（或設每晚自動）|
| `/harvest` | 把重複 ≥2 次的流程變成 skill | 同個流程做過 ≥2 次（忘記也沒關係，capture 會主動問）|
| `/review-doctrine` | 逐條批准 AI 提的行為準則 | `/dream` 說有候選時 |
| `/status` | 看現況（只看不改）| 想知道記了多少 |
| `/schedule-dream` | 建立/列出/刪除每晚自動整理 | 設一次；或要列/刪排程時 |
| `/reset` | 互動式清空記憶、重新開始 | 存了一堆不想要的記憶 |
| `/help` | 列出所有指令 + **怎麼確認通關** | 忘記怎麼用 |

> Claude Code 打 `/指令`；Codex 用同名技能（講「capture 一下 / 跑 dream」即可）。**最詳細的說明打 `/help`。**

幾個重點：
- **多久變一個 skill**：同一流程**出現 ≥2 次（同一天也算，不用等三天）**就會被提議。建技能一律透過**官方 skill-creator**。
- **🧰 固定機械程式 → 存起來下次直接跑（Saved Tool）**：像「截圖到桌面」這種每次都一樣的程式，AI 第一次寫好會問「要不要存起來？」，之後同樣請求**直接執行、不用等它重寫**。這跟 skill 不同：skill 是手冊、tool 是直接跑的程式。
- **工具一直壞掉（例如搜尋失敗）不是做 skill**：那是工具失效 → 自動**硬擋**、改用能用的替代。直接 `/capture` 跟它說「這個工具壞了」，**第 2 次就該擋**。
- **排程不會越堆越多**：只維護一個排程；說「列出排程／刪除排程」就能管理（`CronList` 在 Claude Code CLI 不顯示、要去 VS Code 看）。
- **🎭 AI 人設**：你說「以後叫我老闆／講話直接點」這種「AI 該怎麼當」的話，會存進**人設**（個人層、每次載入），跟「任務偏好」分開。

---

## 記憶是自動記，還是要我打 `/capture`？（重要）

**聊天過程中 AI 就會自己判斷、自動記**——你不一定要打 `/capture`。整套分三層，從「自動」到「保證」：

| 層級 | 要不要動手 | 它做什麼 |
|------|-----------|----------|
| ① 聊天中自動 | **不用** | AI 每輪自己判信心：**高→當場記**（偏好／人設／知識／壞工具）、**中→先存候選**、**低→跳過** |
| ② `/capture` | 你打一次 | 完整掃這段對話、**保證**存檔——想「確定這段一定進去」就打它 |
| ③ 每晚自動 `/ingest-sessions` | 設一次排程 | 回頭讀**真實會話記錄**，把①漏掉的**補回來**（確定性兜底） |

> **為什麼還要 ②③？** 因為①是「盡力而為」，AI 不保證每一輪都記全。所以：**重要的就打 `/capture` 保險，平常交給每晚自動補。**
>
> **兩條鐵則（自動或手動都一樣）**：① **行為準則／技能永遠不會自動生效**——AI 只能「提議」，要你 `/review-doctrine`、`/harvest` 批准才算數；② **低信心（本次產物、暫存檔、閒聊）一律跳過**，不會越記越雜。

---

## 每天怎麼用（最省事版）

```
平常：放著聊就好 —— AI 會自動記重點
重要對話：結束前打 /capture 確保這段存進去
第一次：設一次 /schedule-dream（每晚自動整理 + 補漏）
看到「有 X 條準則待審核」：打 /review-doctrine 批准你認同的
覺得某流程重複做了：打 /harvest（或等它主動問）
```

---

## 常見問題（極簡）

**Q：怎麼確認裝好了 / 在運作？** 打 `/status` 看儀表板；或打 `/help` 看每個指令「怎麼確認通關」。

**Q：會不會把資料傳出去？** 不會。全部存你自己電腦，純文字檔，不需網路、不需資料庫。

**Q：記太多、存了不想要的怎麼辦？** 打 `/reset`——**全程點選、不用打字**：點清哪一層、哪些種類，看完計畫再點一下「確認重置」即可。**動手前一定先備份**，救得回來。

**Q：我只回一句「好 / OK」也會被記到嗎？** 會。它會**回溯前 3-5 輪**，把你拍板的決策**實質**存下來（選了哪個、為什麼、否決了什麼），而不是只存「好」。短但關鍵的決策不會漏，routine 閒聊還是會跳過。

**Q：一定要打 `/capture` 才會記嗎？** 不用。聊天中 AI 會**自動判斷並記下高信心的重點**；`/capture` 是想「確定這段一定進去」時的手動保證，每晚 `/ingest-sessions` 再回讀會話補漏。詳見上面〈記憶是自動記，還是要我打 /capture？〉。

**Q：規矩是 AI 自己亂加的嗎？** 不是。它只能「提議」，**一定要你 `/review-doctrine` 批准**才生效。

**Q：某個技能一直犯同樣的錯？** 它會自己學乖：`/capture` 記下失敗，`/dream` 寫進該技能的「失敗兜底」，下次自動避開。

**Q：conversations / doctrine / persona… 這些英文看不懂？** 翻 [`記憶名詞對照表.md`](記憶名詞對照表.md)（每個分類中文白話＋例子），或打 `/help` 看「記憶分類速查」。

**Q：換電腦？** 把 `~/.ai-memory`（個人腦）複製過去；專案腦跟著專案走。

---

## 只要記住這一件事

> **平常放著聊，AI 會自動記重點；重要的就打 `/capture` 保險；其餘交給每晚自動整理＋補漏。** 忘記指令就打 `/help`。
